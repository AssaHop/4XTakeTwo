# Известные проблемы

Статусы: 🔴 открыт · 🟡 частично решён · 🟢 закрыт. Не удалять закрытые
записи — менять статус и дописывать дату/сессию закрытия, это история.

| ID | Проблема | Файл(ы) | Статус | Найден | Закрыт |
|---|---|---|---|---|---|
| 1 | Победа/поражение не проверяются — `winCondition`/`loseCondition` определены, нигде не вызываются | `scenarios/*.js`, игровой цикл | 🟢 закрыт | сессия 1 | сессия 7 |
| 1a | `dominator.winCondition`/`loseCondition` фильтруют по несуществующему полю `u.alive` | `scenarios/dominator.js` | 🟢 закрыт | сессия 2 | сессия 7 |
| 2 | enemy юниты застревают/idle у острова — **ИСПРАВЛЕНО сессией 3**: подключён `findPath` в `bestStepToward`, см. #22 для деталей механизма и фикса | `ai/fsm/states/attackState.js:bestStepToward` | 🟢 закрыт | сессия 1 | сессия 3 |
| 3 | Нет тестовых карт для проверки LoS вручную | — (новый файл нужен) | 🔴 | сессия 1 | — |
| 4 | Алгоритм роста островов: флудфил от края + вертикальный рост | `islandBuilder.js`, `generateMapByProfile.js` | 🟡 | сессия 1 | частично — появились `applyVerticalIslandGrowth`, `applySurfRim`, `clusterizeTerrain` (сессия 2); связность воды (запертые озёра) закрыта сессией 8, см. #4a |
| 4a | Связность воды не проверялась — генерация красит террейн зонами/вероятностями без флудфила "эта вода реально соединена с открытым морем", отсюда запертые озёра, в которых морской юнит физически не может найти путь наружу (`findPath` не находит маршрут). **ИСПРАВЛЕНО сессией 8**: `ensureWaterConnectivity()` в `islandBuilder.js` — BFS по всем water-тайлам (`surf/water/deep`), находит компоненты связности, самую большую считает открытым морем, все остальные (запертые озёра) закрывает в `land`. Подключено в конце `generateMapByProfile()`. | `utils/islandBuilder.js`, `utils/generateMapByProfile.js` | 🟢 закрыт | сессия 6 (design-заметка), сессия 8 (найдено подтверждено в коде) | сессия 8 |
| 5 | `getAvailableHexes()` использует `map.flat().find()` — O(n²) | `mechanics/units.js` | 🟢 | (до сессии 1) | до сессии 2 — подтверждено в коде: уже `state.mapIndex` |
| 6 | `renderUnits`/`renderMap` вызывались внутри боевой/юнит-логики | `combatLogic.js`, `units.js` | 🟢 | (до сессии 1) | до сессии 2 — подтверждено: централизовано в `events.js:redraw()` |
| 7 | Очередь ходов — бинарный переключатель player1/enemy, не поддерживает много AI | `core/state.js` | 🟢 | (до сессии 1) | до сессии 2 — подтверждено: `turnOrder[]` реализован |
| 8 | `conqueror.js` использует `reef`/`zone` террейны, которые не рендерятся/не обрабатываются в движении/спауне; `turnCount`/`controlledBy` никогда не устанавливаются | `scenarios/conqueror.js` | 🔴 | сессия 2 | — |
| 9 | `savegame.js` — `loadGame()` не применяет загруженный state; даже если бы применял, `JSON.parse` теряет методы класса `Unit` | `core/savegame.js`, `core/game.js` | 🔴 | сессия 2 | — |
| 10 | `ui/input.js` пустой — нет touch-управления, критично для Telegram WebApp | `ui/input.js` | 🔴 | сессия 2 | — |
| 11 | `world/fogOfWar.js` пустой — туман войны не реализован | `world/fogOfWar.js` | 🔴 | сессия 2 | — |
| 12 | Весь каталог `ai/actions/*`, `ai/evaluators/*` и часть `ai/fsm/*` — мёртвый код, не подключён к игровому циклу | см. AGENTS.md §2 | 🔴 (осознанная заготовка, не баг как таковой) | сессия 2 | — |
| 13 | ~27 из ~35 модулей кораблей выставляют флаг через `effect()`, который никем не читается | `core/modules/*Modules.js` | 🟡 частично | сессия 2 | сессия 8 — убран ВТОРОЙ параллельный слой флагов (`core/unitFlags.js`), который дублировал и местами противоречил первому (Still/Ambush имели разный смысл в двух системах); полный глоссарий "что делает каждый модуль и подключён ли" теперь в `docs/content-status.md`. Сами ~26 нереализованных модулей (Seize/Blast/Hard/Splash/Stealth/... + весь Support/Progression) остаются не подключены — это отдельная работа по каждому, не багфикс |
| 14 | Дублирование `cubeRound`/`hexRound` (идентичная формула в двух файлах) | `world/map.js`, `mechanics/hexUtils.js` | 🟢 закрыт | сессия 2 | сессия 8 — `hexRound` экспортирован из `hexUtils.js`, `map.js` теперь `const cubeRound = hexRound` вместо своей копии формулы |
| 15 | Два независимых неиспользуемых файла `getTileFast` | `world/map.js`, `utils/getTileFast.js` | 🟢 закрыт | сессия 2 | сессия 8 — оба нигде не импортировались, удалены: файл `utils/getTileFast.js` целиком, функция+экспорт из `world/map.js` |
| 16 | `combatLogic.js:canAttack()` — мёртвый `console.log` после `return`, ссылается на неопределённую `unit` | `core/combatLogic.js` | 🟢 закрыт | сессия 2 | сессия 8 — строка удалена |
| 17 | Percy/Flee/Charge логика дублируется в трёх местах одновременно | `combatLogic.js`, `gameStateMachine.js`, `aiManager.js` | 🟡 частично | сессия 2 | сессия 8 — `gameStateMachine.js:evaluatePostAction` больше не пересчитывает условия Percy/Flee сама (это делал `combatLogic.js:performAttack`, единственный владелец `actBonusUsed`-гейта), а просто реагирует UI-переходом на уже выставленные `unit.canAct`/`unit.canMove`. Осталась легитимная третья копия в `aiManager.js` — это AI-оркестрация (кого атаковать/куда отступить), а не повтор той же бизнес-логики, поэтому не трогали. |
| 18 | Новые классы кораблей (WSB, WCA, WLC, WSS, AAF, ADB, ATB) рисуются одинаковым кругом — визуально неотличимы | `ui/render.js:drawUnit()` | 🔴 (низкий приоритет, UX) | сессия 2 | — |
| 19 | `core/_turnQueue.js` — мёртвый файл, заменён `state.turnOrder` | `core/_turnQueue.js` | 🟢 закрыт | сессия 2 | сессия 8 — файл удалён, подтверждено что нигде не импортировался |
| 20 | Меню позволяет выбрать сценарий `conqueror`, хотя он сломан (см. #8) | `ui/setup.js` | 🔴 | сессия 2 | — |
| 21 | `weType` хранится как массив (`['Small','Main']` у WCC), но `units.js` конструктор ищет `WeaponTypes[this.weType]` — `Array.toString()` даёт `"Small,Main"`, такого ключа нет → `weaponProfile=undefined` → `atRange` падает на дефолт `1`. Для юнитов с ОДНИМ элементом в `weType` (WBB `['Main']`, WDD `['Small']`) баг не проявляется случайно, т.к. `['Main'].toString()==='Main'` совпадает с ключом. **ИСПРАВЛЕНО сессией 4**: конструктор теперь итерирует по `weaponKeys`, берёт max range среди всех профилей — та же логика, что уже была в `combatLogic.js:getWeaponRange` | `mechanics/units.js`, `core/modules/weaponTypes.js`, `core/classTemplates.js` | 🟢 закрыт | сессия 3 (найдено по факту наблюдения в игре — WCC атакует почти только в упор) | сессия 4 |
| 22 | **ПОДТВЕРЖДЕНО вторым логом (с первого хода партии).** `enemy0` (WBB на `(-10,3,7)`) каждый ход двигается, но никогда не приближается к скоплению боя у `q≈8-10` — мечется вдоль побережья острова (`(-11,4,7)`→`(-10,3,7)`→`(-10,1,9)`, фактически назад на одном из шагов). Причина — `bestStepToward()` чисто жадный: на каждом ходу пересчитывает `getAvailableHexes()` (только гексы в пределах `moRange`) и берёт ближайший по ПРЯМОЙ `hexDistance` к цели, без памяти о пути и без учёта препятствий за пределами текущего шага. `findPath` (A*, корректно обошёл бы препятствие) существовал в `mechanics/pathfinding.js`, но не вызывался. **ИСПРАВЛЕНО сессией 3**: `bestStepToward` теперь строит полный маршрут через `findPath` и берёт из него самую далёкую достижимую за этот ход точку, с fallback на старое поведение если путь не найден | `ai/fsm/states/attackState.js:bestStepToward` | 🟢 закрыт | сессия 1 (баг), сессия 3 (подтверждён логом и исправлен) | сессия 3 |
| 24 | `hasLineOfSight` использовал `getHexLine` → `hexRound`, который при "граничном" шаге (lerp-точка точно на стыке двух гексов) выбирал один гекс через округление — что делало блокировку LoS недетерминированной (зависело от направления линии, не от дизайна карты). **ИСПРАВЛЕНО сессией 4**: добавлены `hexRoundDual`/`getHexLineDual` в `hexUtils.js`; `hasLineOfSight` теперь использует `getHexLineDual` и считает шаг блокирующим только если ОБА кандидата на стыке — блокирующий террейн (если хотя бы один проходим — не блокирует) | `mechanics/hexUtils.js`, `mechanics/lineOfSight.js` | 🟢 закрыт | сессия 4 | сессия 4 |
| 23 | `findPath` теперь подключен (сессия 3), но только для одной заранее выбранной цели-юнита. Архитектурный потолок: нужен обобщённый слой "сканируй карту → собери кандидатов на цель (юниты+города+зоны) → оцени → найди путь". **Частично решено сессией 4**: capture points добавлены как цели в `decideCaptureAction` (fallback когда нет врагов в зоне), Flee обновлён. **Сессия 5**: CP теперь конкурирует с атакой в единой шкале — `cpScore` возвращается из `decideCaptureAction` и сравнивается с `best.score` от врагов. Осталось: fine-tuning весов и focus fire. | `ai/fsm/states/attackState.js`, `ai/aiManager.js`, `core/captureLogic.js` | 🟡 | сессия 3 | — |
| 25 | AI юниты уходили в idle если расстояние до ближайшего врага > ~16 гексов: `score = 50 - 3×dist` давало отрицательный результат, а `if (best.score < 0) return idle` полностью блокировало движение. **ИСПРАВЛЕНО сессией 5**: (1) штраф за дистанцию снижен с `×3` до `×1`; (2) порог `best.score < 0` убран — score определяет КАКУЮ цель выбрать, не двигаться ли вообще. | `ai/fsm/states/attackState.js:scoreTarget` | 🟢 закрыт | сессия 5 (подтверждён логом: enemy0 WBB idle 4 хода) | сессия 5 |
| 26 | AI юниты с дальним оружием (WBB/WDD/WCC, atRange=6) подходили вплотную к цели вместо остановки на оптимальной дистанции. **ИСПРАВЛЕНО сессией 5**: `bestStepToward` принимает `optimalRange` (по умолчанию 0); в `decideAction` передаётся `unit.atRange - 1` — юнит останавливается на дистанции `atRange-1` от цели в первом проходе, при невозможности — fallback на обычное движение. | `ai/fsm/states/attackState.js:bestStepToward` | 🟢 закрыт | сессия 5 | сессия 5 |
| 27 | `dangerBonus` в `scoreTarget` был захардкожен только для WCC/WDD/WBB и не охватывал авиацию/WSB/WCA/WSS. `atDamage` для ADB/ATB/AAF отсутствовал (фоллбэк давал 1 урона — как слабейший юнит). WDD имел только `['Small']` в weType, без Torp. **ИСПРАВЛЕНО сессией 6**: `dangerScore` добавлен во все шаблоны в `classTemplates.js` (WCA=45, WSB=35, WSS=30, авиация=35/20, WCC=20, WDD=15, WBB/WLC=10); `atDamage` добавлен ADB=5/ATB=4/AAF=3; `'Torp'` добавлен WDD.weType; `scoreTarget` заменил хардкод на `target.dangerScore || 0`. Шаг B (SimpleAgent.evalAttack): штраф -60 если цель в своей зоне атаки и убивает нас ответным ударом. | `core/classTemplates.js`, `ai/fsm/states/attackState.js:scoreTarget` | 🟢 закрыт | сессия 5 (найдено при анализе) | сессия 6 |
| 28 | AAF/ADB/ATB не имели `modules: ['Air']` → `ignoresObstacles` не выставлялся → `getAvailableHexes()` = [] → авиация атаковала дальним оружием с точки спауна без перемещения. Ломало ключевое баланс-решение "короткая дальность = самолёт должен подлететь". **ИСПРАВЛЕНО сессией 7**: добавлен `modules: ['Air']` в AAF/ADB/ATB в `classTemplates.js`. | `core/classTemplates.js`, `mechanics/units.js:getAvailableHexes` | 🟢 закрыт | сессия 6 (найдено при анализе, отложено) | сессия 7 |
